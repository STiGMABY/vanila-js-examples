// Напишите функцию findCouple(array, number),
// которая ищет в массиве все пары целых чисел, сумма которых равна заданному значению.

let num = 5
let arr = [1, 6, 5, 2, 7, 5, 1, 4, 3, 9, 8, 11, 10, 18]

function findCouple(array, number) {
  //return array.
}

console.log(findCouple(arr, num)) // Ваша пара чисел: 1 + 4, Ваша пара чисел: 2 + 3
